%section.twelve.columns.offset-by-two.project-container
  %article.colophon.twelve.columns
    %p
      -@colophon.each do |post|
        %h3=post.title
        %p=find_and_preserve(markdown(post.content))