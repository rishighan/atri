%section.project-container.twelve.columns.offset-by-two
  %article.twelve.columns
    %ul
      -@projects.each do |p|
        %li
          %h3=link_to "#{p.title}", project_path(p)
          %span=p.excerpt

